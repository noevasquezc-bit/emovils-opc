# Emovils OPC — Dependencias del Sistema
# Instalar con: pip install -r requirements.txt

# IA / Agentes
anthropic>=0.25.0
openai>=1.0.0

# TTS — voz de Monserrat (Google TTS, gratis)
gTTS>=2.5.0

# APIs externas
requests>=2.31.0
# PayPal — usado en RD en lugar de Stripe
# (usamos requests directamente con PayPal REST API v2, sin SDK)

# Configuración
python-dotenv>=1.0.0

# Servidor web (para webhooks)
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# QR codes
qrcode[pil]>=7.4.2
Pillow>=10.0.0

# Utilidades
python-dateutil>=2.8.2
pytz>=2024.1

# Automatización temporal (opc/scheduler.py — reportes, follow-ups, quincena)
apscheduler>=3.10

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0
responses>=0.25.0

# Excel parsing (Intelcia files)
openpyxl>=3.1.0
